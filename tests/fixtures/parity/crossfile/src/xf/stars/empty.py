"""A lone star import supplying nothing: the "0 names" wording, at DECLARED."""

from xf.stars.other import *


def local_only() -> int:
    """Use nothing the star could have supplied."""
    return 1
